@AGENTS.md

Claude Code reads this file; Codex reads `AGENTS.md`. Both get the same rules
through the import above. Documentation conventions and commands are in
`docs/README.md`; the skill `.claude/skills/usl-docs-authoring` says how to add
or change a page.
