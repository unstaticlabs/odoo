---
title: States
type: reference
description: The lifecycle values of every product record, in the order the product moves through them.
lang: en
persona: everyone
source:
  - custom-addons/rebuild_account_migration
  - custom-addons/usl_access_control
  - custom-addons/usl_accounting
  - custom-addons/usl_b2c
  - custom-addons/usl_b2c_ingest
  - custom-addons/usl_documents
  - custom-addons/usl_documents_accounting
  - custom-addons/usl_documents_b2c
  - custom-addons/usl_expense_batch
  - custom-addons/usl_feedback
  - custom-addons/usl_home
  - custom-addons/usl_locale
  - custom-addons/usl_platform_billing
  - custom-addons/usl_pocketid
  - custom-addons/usl_project
  - custom-addons/usl_sign
  - custom-addons/usl_tese_payroll
generated: true
---

<!-- Generated by scripts/docs-reference from the product's registry. Change the code, then run `make docs-reference`. -->

# States

The lifecycle values of every product record, in the order the product moves through them.

## Accounting Hygiene Issue: Status

- **Open** (`open`)
- **Resolved** (`resolved`)
- **Dismissed** (`dismissed`)

## Agent API Credential: Status

- **Active** (`active`)
- **Expired** (`expired`)
- **Revoked** (`revoked`)

## Autonomous Agent: State

- **Active** (`active`)
- **Suspended** (`suspended`)

## B2C Fulfilment and COGS Event: State

- **Pending** (`pending`)
- **Partially fulfilled** (`partially_fulfilled`)
- **Fulfilled** (`fulfilled`)
- **Refunded** (`refunded`)
- **Cancelled** (`cancelled`)
- **Unknown** (`unknown`)

## B2C Import Batch: State

- **Draft** (`draft`)
- **Read** (`parsed`)
- **Resolved** (`resolved`)
- **Simulated** (`simulated`)
- **Applied** (`applied`)

## B2C Import Source File: State

- **Not read yet** (`pending`)
- **Recognised** (`recognised`)
- **Unrecognised** (`unreadable`)

## B2C Payment, Refund and Fee Event: State

- **Pending** (`pending`)
- **Authorized** (`authorized`)
- **Captured** (`captured`)
- **Settled** (`settled`)
- **Failed** (`failed`)
- **Refunded** (`refunded`)
- **Cancelled** (`cancelled`)
- **Unknown** (`unknown`)

## Bank Statement Review Exception: State

- **Open** (`open`)
- **Resolved** (`resolved`)

## Canonical B2C Order: State

- **Pending** (`pending`)
- **Confirmed** (`confirmed`)
- **Partially fulfilled** (`partially_fulfilled`)
- **Fulfilled** (`fulfilled`)
- **Partially refunded** (`partially_refunded`)
- **Refunded** (`refunded`)
- **Cancelled** (`cancelled`)
- **Unknown** (`unknown`)

## Content Platform Billing Session: State

- **Draft** (`draft`)
- **Ready** (`ready`)
- **Generated** (`generated`)
- **Posted** (`posted`)
- **Paid** (`paid`)
- **Cancelled** (`cancelled`)

## Content Platform Payout: State

- **Draft** (`draft`)
- **Generated** (`generated`)
- **Posted** (`posted`)
- **Paid** (`paid`)
- **Cancelled** (`cancelled`)

## Create Expense Batch: Batch readiness

- **Ready** (`ready`)
- **Needs information** (`incomplete`)

## Deferred Expense and Revenue Schedule: State

- **Draft** (`draft`)
- **Running** (`running`)
- **Closed** (`closed`)

## Deferred Expense and Revenue Schedule Line: State

- **Scheduled** (`scheduled`)
- **Posted** (`posted`)

## Destination VAT Return: State

- **Draft** (`draft`)
- **Ready to file** (`ready`)
- **Filed** (`filed`)

## Document Ingestion Operation: State

- **Pending** (`pending`)
- **Uploading** (`uploading`)
- **Processing** (`processing`)
- **Archived** (`archived`)
- **Duplicate reused** (`duplicate`)
- **Failed** (`failed`)

## Electronic Invoice Reception Evidence: Status

- **Processing** (`received`)
- **Ready for Review** (`bill_created`)
- **Rejected** (`rejected`)
- **Duplicate Ignored** (`duplicate`)
- **Needs Attention** (`technical_error`)

## Expense Bank Match Candidate: State

- **Available** (`available`)
- **Accepted** (`accepted`)
- **No longer available** (`unavailable`)

## Expense Batch: Batch state

- **Open** (`open`)
- **Archived** (`archived`)

## Expense Batch: Expense progress

- **No expenses** (`empty`)
- **Has drafts** (`draft`)
- **Waiting approval** (`submitted`)
- **Ready to post** (`approved`)
- **All posted** (`posted`)
- **All paid** (`paid`)
- **Returned only** (`refused`)

## Expense Batch: Batch readiness

- **Ready** (`ready`)
- **Needs information** (`incomplete`)

## Foreign-Currency Settlement: State

- **Settled** (`settled`)
- **Reversed** (`reversed`)

## Independent Signature Validation Run: Status

- **Valid** (`valid`)
- **Invalid** (`invalid`)
- **Indeterminate** (`indeterminate`)

## Learned linked receipt pattern: State

- **Learning** (`learning`)
- **Active** (`active`)
- **Paused** (`paused`)
- **Blocked** (`blocked`)

## Linked expense receipt retrieval: State

- **Selection required** (`selection_required`)
- **Queued** (`queued`)
- **Running** (`running`)
- **Retrying** (`retrying`)
- **Succeeded** (`succeeded`)
- **Needs attention** (`needs_attention`)
- **Superseded** (`superseded`)
- **Dismissed** (`dismissed`)

## Linked receipt host: State

- **Provisional** (`provisional`)
- **Active** (`active`)
- **Blocked** (`blocked`)

## Monthly B2C Accounting Session: State

- **Draft** (`draft`)
- **Reviewed** (`reviewed`)
- **Locked** (`locked`)

## One-time Strong Signature Ceremony: State

- **Authorization challenge** (`challenge`)
- **Pocket ID authorization** (`authorizing`)
- **Authorized** (`authorized`)
- **Completed** (`completed`)
- **Expired** (`expired`)
- **Failed** (`failed`)
- **Cancelled** (`revoked`)

## Platform Payout Bank Allocation: State

- **Linked** (`linked`)
- **Reconciled** (`reconciled`)
- **Blocked** (`blocked`)

## Product Feedback Assistant Run: State

- **Queued** (`queued`)
- **Submitted** (`submitted`)
- **Completed** (`completed`)
- **Error** (`error`)
- **Stale** (`stale`)

## Provider-neutral External Signature Journey: State

- **Waiting for external signature** (`waiting`)
- **Signed document imported** (`imported`)
- **Validated** (`validated`)
- **Rejected** (`rejected`)
- **Cancelled** (`cancelled`)

## Received Bank Export: State

- **Received** (`received`)
- **Processing** (`processing`)
- **Processed** (`done`)
- **Needs attention** (`attention`)
- **Import failed** (`failed`)

## Sign Request: State

- **Sent** (`0_sent`)
- **Draft** (`draft`)
- **Draft** (`1_draft`)
- **Ready** (`ready`)
- **Signed** (`2_signed`)
- **Sent** (`sent`)
- **Cancelled** (`3_cancel`)
- **Viewed** (`viewed`)
- **Partially signed** (`partial`)
- **Waiting for enrolment** (`waiting_enrollment`)
- **Waiting for external signature** (`waiting_external`)
- **Signed document to import** (`signed_to_import`)
- **Validation in progress** (`validating`)
- **Completed** (`completed`)
- **Archived external signing record** (`external_archived`)
- **Evidence incomplete** (`evidence_incomplete`)
- **Validation failed** (`validation_failed`)
- **Declined** (`declined`)
- **Expired** (`expired`)
- **Cancelled** (`cancelled`)
- **Action required** (`action_required`)

## Sign Request Value: State

- **Draft** (`draft`)
- **Notified** (`notified`)
- **Viewed** (`viewed`)
- **Authorized** (`authorized`)
- **Signed** (`signed`)
- **Recorded as signed externally** (`external_recorded`)
- **Declined** (`declined`)
- **Expired** (`expired`)
- **Cancelled** (`cancelled`)

## Signed Daily Signature Evidence Manifest: State

- **Signed** (`signed`)
- **Signing failed** (`failed`)

## Signing Capability Status: Status

- **Ready** (`ready`)
- **Degraded** (`degraded`)
- **Not configured** (`not_configured`)
- **Unreachable** (`unreachable`)
- **Action required** (`action_required`)

## Strong Signer Identity Enrolment: State

- **Waiting for account connection** (`pending_pocket`)
- **Ready for review** (`pending_review`)
- **Ready** (`active`)
- **Revoked** (`revoked`)

## TESE Payroll Record: State

- **Draft** (`draft`)
- **Prepared** (`prepared`)
- **Ready to post** (`to_post`)
- **To reconcile** (`to_reconcile`)
- **Settled** (`paid`)
- **Cancelled** (`cancelled`)

## USL Accounting Assurance Decision: State

- **Draft** (`draft`)
- **Recorded** (`recorded`)
- **Superseded** (`superseded`)

## USL Accounting Closing Control: Status

- **Passed** (`pass`)
- **Information** (`info`)
- **Warning** (`warning`)
- **Blocking** (`block`)
- **Technical Failure** (`technical_error`)
- **Not Applicable** (`not_applicable`)

## USL Accounting Closing Workspace: State

- **Open** (`open`)
- **Preparing** (`preparing`)
- **Blocked** (`blocked`)
- **Internal Review** (`internal_review`)
- **Accountant Review** (`accountant_review`)
- **Ready to Close** (`ready`)
- **Closed and Locked** (`closed`)
- **Archived** (`archived`)

## USL French Declaration Obligation: Status

- **To Prepare** (`to_prepare`)
- **Data Missing** (`data_missing`)
- **Ready for Internal Review** (`internal_review`)
- **Ready for Accountant Review** (`accountant_review`)
- **Accountant Reviewed** (`accountant_reviewed`)
- **Ready to File Externally** (`ready_to_file`)
- **Filed Externally** (`filed`)
- **Paid / Refunded** (`paid`)
- **Archived** (`archived`)
- **Blocked** (`blocked`)
- **Not Applicable** (`not_applicable`)

## Verified Personal Email Address: State

- **Pending verification** (`pending`)
- **Verified** (`verified`)
