---
title: "Fields: usl_project"
type: reference
description: Every field `usl_project` defines or adds to Odoo, by model, with its label, type and meaning.
lang: en
persona: everyone
source:
  - custom-addons/usl_project
generated: true
---

<!-- Generated by scripts/docs-reference from the product's registry. Change the code, then run `make docs-reference`. -->

# Fields: usl_project

Every field `usl_project` defines or adds to Odoo, by model, with its label, type and meaning.

## Project

`project.project`, extended by this module (defined by `usl_documents`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Usl Show Company On Card** (`usl_show_company_on_card`) | boolean |  |  |

## Task

`project.task`, extended by this module (defined by `usl_documents`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Planned Start** (`planned_date_begin`) | datetime |  | The date and time when work is planned to start. Together with the deadline, it defines the task's planned range. |
| **Dependency Schedule Warning** (`usl_dependency_date_warning`) | char |  |  |

## Task Stage

`project.task.type`, extended by this module (defined by `usl_feedback`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Reactivation Role** (`usl_reactivation_role`) | selection |  | For a personal stage, identifies where deferred tasks wait and where they return when work becomes actionable. Values: **Inbox destination**, **Later source**. |
