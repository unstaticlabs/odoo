---
title: "Fields: usl_b2c_ingest"
type: reference
description: Every field `usl_b2c_ingest` defines or adds to Odoo, by model, with its label, type and meaning.
lang: en
persona: everyone
source:
  - custom-addons/usl_b2c_ingest
generated: true
---

<!-- Generated by scripts/docs-reference from the product's registry. Change the code, then run `make docs-reference`. -->

# Fields: usl_b2c_ingest

Every field `usl_b2c_ingest` defines or adds to Odoo, by model, with its label, type and meaning.

## B2C Commercial Channel

`b2c.channel`, extended by this module (defined by `usl_b2c`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Clearing journals** (`clearing_journal_ids`) | many2many → `account.journal` |  | Where this channel's receipts are held until it pays them out, one per currency it collects in. |
| **Commission product** (`fee_product_id`) | many2one → `product.product` |  | The product the channel's commission is bought as, so it reaches the commissions account rather than a general expense. |
| **Channel operator** (`operator_partner_id`) | many2one → `res.partner` |  | Who runs this channel and bills its commission. Naming it is what makes the reverse charge on its services apply. |
| **Printful Store** (`printful_store_id`) | char |  | The Printful store that fulfils this channel. Configuration, not code. |
| **Carriage product** (`shipping_product_id`) | many2one → `product.product` |  | The product that carries what customers are charged to ship, so it reaches the carriage account rather than the revenue account. |

## B2C Import Batch

`b2c.import.batch`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Activity Plans** (`activity_plans_ids`) | many2many → `mail.activity.plan` |  |  |
| **Advances held** (`advance_payment_ids`) | many2many → `account.payment` |  | What customers paid for orders whose goods have not left yet. |
| **Advisory Issue Count** (`advisory_issue_count`) | integer |  |  |
| **Applied Order Count** (`applied_order_count`) | integer |  |  |
| **Sales created** (`applied_sale_ids`) | many2many → `sale.order` |  |  |
| **Blocking Issue Count** (`blocking_issue_count`) | integer |  |  |
| **Sales cancelled** (`cancelled_sale_ids`) | many2many → `sale.order` |  |  |
| **Company** (`company_id`) | many2one → `res.company` | yes |  |
| **Conflicting Count** (`conflicting_count`) | integer |  |  |
| **Credit notes** (`credit_note_ids`) | many2many → `account.move` |  |  |
| **Commission bills** (`fee_bill_ids`) | many2many → `account.move` |  |  |
| **File Count** (`file_count`) | integer |  |  |
| **Files** (`file_ids`) | one2many → `b2c.import.file` |  |  |
| **Fulfilment Count** (`fulfilment_count`) | integer |  |  |
| **Fulfilments recorded** (`fulfilment_event_ids`) | many2many → `b2c.fulfilment.event` |  | What the supplier shipped, as this drop read it. What each one cost is settled against the wallet from here. |
| **Invoices** (`invoiced_move_ids`) | many2many → `account.move` |  |  |
| **Findings** (`issue_ids`) | one2many → `b2c.import.issue` |  |  |
| **Known Order Count** (`known_order_count`) | integer |  |  |
| **Line Count** (`line_count`) | integer |  |  |
| **Mapped Line Count** (`mapped_line_count`) | integer |  |  |
| **Name** (`name`) | char | yes |  |
| **New Order Count** (`new_order_count`) | integer |  |  |
| **Order Count** (`order_count`) | integer |  |  |
| **Period End** (`period_end`) | date |  |  |
| **Period Start** (`period_start`) | date |  |  |
| **Report** (`report`) | text |  |  |
| **Row Count** (`row_count`) | integer |  |  |
| **Rows** (`row_ids`) | one2many → `b2c.import.row` |  |  |
| **State** (`state`) | selection | yes | Values: **Draft**, **Read**, **Resolved**, **Simulated**, **Applied**. |
| **Supplier currency** (`supplier_currency_id`) | many2one → `res.currency` |  |  |
| **Supplier Internal Cost** (`supplier_internal_cost`) | monetary |  | What the supplier charged for orders no sale in this drop matched. An order that fulfils no sale at all is marketing or prototyping, which is a cost and never revenue; check the untied fulfilments before reading this as that. |
| **Supplier Sales Cost** (`supplier_sales_cost`) | monetary |  | What the supplier charged to fulfil sales in this drop. |
| **Unmapped Line Count** (`unmapped_line_count`) | integer |  |  |
| **Wallet Balance** (`wallet_balance`) | monetary |  | What is left of the prepaid balance the supplier draws on. |
| **Wallet documents** (`wallet_move_ids`) | many2many → `account.move` |  |  |

## B2C Import Source File

`b2c.import.file`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Batch** (`batch_id`) | many2one → `b2c.import.batch` | yes |  |
| **Checksum** (`checksum`) | char |  |  |
| **Content** (`content`) | binary | yes |  |
| **Recognised as** (`format_id`) | selection |  |  |
| **Line Count** (`line_count`) | integer |  |  |
| **Name** (`name`) | char | yes |  |
| **Note** (`note`) | text |  |  |
| **Order Count** (`order_count`) | integer |  |  |
| **Provider** (`provider`) | selection |  | Values: **Etsy**, **Legacy Goodboys / Medusa**, **Medusa**, **Stripe**, **Revolut Merchant**, **Printful**, **Manual**, **Other**. |
| **Row Count** (`row_count`) | integer |  |  |
| **Schema Digest** (`schema_digest`) | char |  |  |
| **State** (`state`) | selection | yes | Values: **Not read yet**, **Recognised**, **Unrecognised**. |

## B2C Import Finding

`b2c.import.issue`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Batch** (`batch_id`) | many2one → `b2c.import.batch` | yes |  |
| **External Order** (`external_order_id`) | char |  |  |
| **Kind** (`kind`) | selection | yes | Values: **Unrecognised file**, **Order identity belongs to another channel**, **Odoo holds the same order twice**, **Files disagree about the same order**, **Channel totals no longer add up**, **Line without an order**, **Order-level money is not in the export**, **The channel sold something Odoo does not know**, **The product exists but not in this variant**, **The name points at more than one product**, **A mapping was derived from the catalogue**, **A supplier fulfilment belongs to no known sale**, **The export does not say where the goods went**, **The delivery could not be completed**, **A destination is taxed by the wrong fiscal position**, **A product does not state one sales tax**, **A tax would be added to the price already paid**, **Carriage has nowhere of its own to go**, **Revenue has nowhere of its own to go**, **Destination VAT does not accrue where it belongs**, **A channel operator has no tax number**, **A fiscal position states a retired rate**, **A cancelled order has already shipped**, **Part of an order was refunded, without saying which part**, **The supplier drew more than the wallet holds**. |
| **Name** (`name`) | char | yes |  |
| **Note** (`note`) | text |  |  |
| **Proposal** (`proposal`) | json |  | What a resolution action needs: the product and the attribute it lacks. |
| **Proposed Value** (`proposed_value`) | char |  | The attribute value to add, spelled the way the catalogue spells its siblings. |
| **Row** (`row_id`) | many2one → `b2c.import.row` |  |  |
| **Severity** (`severity`) | selection | yes | Values: **Blocking**, **Advisory**. |

## B2C Import Source Row

`b2c.import.row`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Alias** (`alias_id`) | many2one → `b2c.product.alias` |  |  |
| **Batch** (`batch_id`) | many2one → `b2c.import.batch` | yes |  |
| **External Line** (`external_line_id`) | char |  |  |
| **External Order** (`external_order_id`) | char | yes |  |
| **File** (`file_id`) | many2one → `b2c.import.file` |  |  |
| **Format** (`format_id`) | char | yes |  |
| **Fulfilment Evidence** (`fulfilment_evidence`) | char |  |  |
| **Fulfils** (`fulfilment_of_row_id`) | many2one → `b2c.import.row` |  | The order row this supplier fulfilment belongs to. |
| **Grain** (`grain`) | selection | yes | Values: **Order**, **Order line**. |
| **Mapping** (`mapping`) | selection |  | Values: **A confirmed alias says so**, **Matched exactly, alias not written yet**, **No product yet**, **Not a line**. |
| **Note** (`note`) | char |  |  |
| **Occurred At** (`occurred_at`) | datetime |  |  |
| **Order** (`order_id`) | many2one → `b2c.order` |  |  |
| **Source payload** (`payload`) | json |  | The unmodified source row. It can contain customer personal data. |
| **Payload Digest** (`payload_digest`) | char | yes |  |
| **Product** (`product_id`) | many2one → `product.product` |  |  |
| **Provider** (`provider`) | selection | yes | Values: **Etsy**, **Legacy Goodboys / Medusa**, **Medusa**, **Stripe**, **Revolut Merchant**, **Printful**, **Manual**, **Other**. |
| **Resolution** (`resolution`) | selection |  | Values: **Not in Odoo yet**, **Already in Odoo**, **Conflicts with what Odoo holds**, **Line without an order**, **Supplier fulfilment**. |
| **Source Row Number** (`source_row_number`) | integer |  |  |
| **Normalised values** (`values`) | json |  |  |

## Canonical B2C Order

`b2c.order`, extended by this module (defined by `usl_b2c`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Superseded by** (`superseded_by_id`) | many2one → `b2c.order` |  | The order that states this same sale in full. A sale recorded twice is one sale, and the poorer record stops counting. |

## Destination VAT Return

`b2c.oss.return`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Base Amount** (`base_amount`) | monetary |  |  |
| **Company** (`company_id`) | many2one → `res.company` | yes |  |
| **Computed At** (`computed_at`) | datetime |  |  |
| **Currency** (`currency_id`) | many2one → `res.currency` |  |  |
| **Countries** (`line_ids`) | one2many → `b2c.oss.return.line` |  |  |
| **Name** (`name`) | char |  |  |
| **Quarter** (`quarter`) | selection | yes | Values: **January to March**, **April to June**, **July to September**, **October to December**. |
| **Registered for the One Stop Shop** (`registered`) | boolean |  | Until this is true, the VAT other Member States are owed accrues to the account below instead of being declared as collected. |
| **State** (`state`) | selection | yes | Values: **Draft**, **Ready to file**, **Filed**. |
| **Vat Amount** (`vat_amount`) | monetary |  |  |
| **Year** (`year`) | integer | yes |  |

## Destination VAT Return Line

`b2c.oss.return.line`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Base Amount** (`base_amount`) | monetary |  |  |
| **Company** (`company_id`) | many2one → `res.company` |  |  |
| **Country** (`country_id`) | many2one → `res.country` | yes |  |
| **Currency** (`currency_id`) | many2one → `res.currency` |  |  |
| **Rate (%)** (`rate`) | float |  |  |
| **Return** (`return_id`) | many2one → `b2c.oss.return` | yes |  |
| **Vat Amount** (`vat_amount`) | monetary |  |  |

## B2C Supply Settlement

`b2c.supply.settlement`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Batch** (`batch_id`) | many2one → `b2c.import.batch` |  |  |
| **Cogs Amount** (`cogs_amount`) | monetary |  | What of this fulfilment's cost this document settled. |
| **Company Cogs Amount** (`company_cogs_amount`) | monetary |  |  |
| **Company currency** (`company_currency_id`) | many2one → `res.currency` |  |  |
| **Company** (`company_id`) | many2one → `res.company` | yes |  |
| **Currency** (`currency_id`) | many2one → `res.currency` | yes |  |
| **Fulfilment** (`event_id`) | many2one → `b2c.fulfilment.event` | yes |  |
| **Document** (`move_id`) | many2one → `account.move` | yes |  |

## Company

`res.company`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Position for B2C sales outside the Union** (`usl_b2c_export_position_id`) | many2one → `account.fiscal.position` |  | A sale outside the Union bears no VAT, but so does a business buying services there, for a different reason. Naming the position for consumer goods keeps the two apart on the invoice. Leave it empty to let the fiscal positions decide. |
| **Supply for no sale** (`usl_b2c_internal_supply_product_id`) | many2one → `product.product` |  | What a fulfilment answering to no sale is bought as. It is the cost of finding out what to sell, never the cost of something sold. |
| **Registered for the One Stop Shop** (`usl_b2c_oss_registered`) | boolean |  | Until this is true, the VAT other Member States are owed accrues to the account below instead of being declared as collected. |
| **Destination VAT collected** (`usl_b2c_oss_vat_account_id`) | many2one → `account.account` |  | Where VAT owed to another Member State goes once the shop is registered to remit it. |
| **Destination VAT to regularise** (`usl_b2c_pending_vat_account_id`) | many2one → `account.account` |  | Where VAT owed to another Member State accrues while the shop is not registered to remit it. |
| **Print-on-demand supplier** (`usl_b2c_supply_partner_id`) | many2one → `res.partner` |  | Who fulfils print-on-demand orders and draws on the wallet for them. |
| **Supply of goods sold** (`usl_b2c_supply_product_id`) | many2one → `product.product` |  | What the supplier's fulfilment of a sale is bought as, so it reaches the cost-of-sales account. |
| **Supplier wallet** (`usl_b2c_wallet_journal_id`) | many2one → `account.journal` |  | The journal holding the prepaid balance the supplier draws on. Its account is what a top-up pays into and what a month's supply empties. |
