---
title: "Fields: usl_documents"
type: reference
description: Every field `usl_documents` defines or adds to Odoo, by model, with its label, type and meaning.
lang: en
persona: everyone
source:
  - custom-addons/usl_documents
generated: true
---

<!-- Generated by scripts/docs-reference from the product's registry. Change the code, then run `make docs-reference`. -->

# Fields: usl_documents

Every field `usl_documents` defines or adds to Odoo, by model, with its label, type and meaning.

## Bank Statement Line

`account.bank.statement.line`, extended by this module (defined by `usl_accounting`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Journal Entry

`account.move`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Payment

`account.payment`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Employee

`hr.employee`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Expense

`hr.expense`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Attachment

`ir.attachment`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Documents archive mode** (`usl_documents_archive_mode`) | selection |  | Values: **Mandatory retention**, **Automatic**, **Keep on request**, **Excluded**. |
| **Documents role** (`usl_documents_document_role`) | selection |  | Values: **Evidence**, **Library**, **Background**. |
| **Documents ledger state** (`usl_documents_ledger_state`) | selection |  | Values: **Policy not resolved**, **Archive pending**, **Archived evidence**, **Archived library**, **Archived background**, **Native only — keep on request**, **Explicitly excluded**, **Blocked failure**. |
| **Documents origin** (`usl_documents_origin`) | selection |  | Values: **Documents workspace**, **Direct record upload**, **Chatter**, **Portal**, **Generated final output**, **Generated transient output**, **External Paperless intake**, **Migration**, **Backfill**. |
| **Documents policy reason** (`usl_documents_policy_reason`) | char |  |  |

## Product Document

`product.document`, extended by this module (defined by `Odoo`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Documents archive mode** (`usl_documents_archive_mode`) | selection |  |  |
| **Documents role** (`usl_documents_document_role`) | selection |  |  |
| **Documents ledger state** (`usl_documents_ledger_state`) | selection |  |  |
| **Documents origin** (`usl_documents_origin`) | selection |  |  |
| **Documents policy reason** (`usl_documents_policy_reason`) | char |  |  |

## Project

`project.project`, defined by this module.

A top-level container used to organize and track a group of related project.tasks. Provides a high-level overview of progress, budget, and time spent on a specific initiative or client.

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Task

`project.task`, defined by this module.

A specific piece of work to be completed within a project. Tasks can be assigned to employees, tracked through stages (e.g., In Progress, Done), and have logged timesheets.

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Company

`res.company`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Config Settings

`res.config.settings`, extended by this module (defined by `rebuild_account_migration`). Transient: the record lives only while its dialog is open.

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Paperless Health** (`paperless_health`) | char |  |  |
| **Last successful synchronization** (`paperless_last_sync`) | char |  |  |
| **Paperless public URL** (`paperless_public_url`) | char |  | Secured user-facing URL used for Open in Paperless. |
| **Paperless service user ID** (`paperless_service_user_id`) | integer |  | Dedicated non-human Paperless owner used by server-side integration. |
| **Paperless streaming timeout** (`paperless_stream_timeout`) | integer |  | Maximum server-to-server read time for one streamed binary request. |
| **Paperless timeout** (`paperless_timeout`) | integer |  |  |
| **Paperless integration token** (`paperless_token`) | char |  |  |
| **Paperless internal URL** (`paperless_url`) | char |  | Container-network URL used only by the Odoo server. |

## Contact

`res.partner`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## User

`res.users`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Archived Document

`usl.document`, extended by this module (defined by `usl_access_control`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Access Scope** (`access_scope`) | selection | yes | Linked-record documents are visible only to Documents users who can read at least one active related Odoo record. Values: **Company policy**, **Linked record access**. |
| **Accounting Evidence** (`accounting_evidence`) | boolean |  | Mark this document as supporting evidence for bookkeeping, tax, or audit work. With Accounting evidence privacy, it becomes available read-only to Accounting Evidence Readers. The document is also easier to retrieve in accounting filters and is put on retention hold if Paperless reports it in Trash. Changing this setting resynchronizes archive permissions. |
| **Activity Plans** (`activity_plans_ids`) | many2many → `mail.activity.plan` |  |  |
| **Everywhere** (`all_text`) | char |  | Search OCR content, title, Paperless metadata, additional details, and accessible linked Odoo records. |
| **Added** (`archive_added_at`) | datetime |  | Original creation time supplied with imported documents. New uploads use their submission time. |
| **Archive Checksum** (`archive_checksum`) | char |  |  |
| **Document content** (`archive_text`) | char |  | Search title, OCR text, archive metadata, and Paperless identity. |
| **Availability State** (`availability_state`) | selection | yes | Updated automatically from Paperless processing, reconciliation, trash and restore operations, and permission checks. It cannot be changed manually. Values: **Available**, **Processing**, **Missing from Paperless**, **Trashed in Paperless**, **Permanently deleted**, **Permission synchronization failed**, **Processing failed**. |
| **Checksum** (`checksum`) | char |  |  |
| **Company** (`company_id`) | many2one → `res.company` |  |  |
| **Confidentiality** (`confidentiality`) | selection | yes | Values: **Internal**, **Accounting evidence**, **HR restricted**, **Creator private**. |
| **Correspondent** (`correspondent_id`) | many2one → `usl.paperless.correspondent` |  |  |
| **Correspondent Name** (`correspondent_name`) | char |  |  |
| **Current Version Label** (`current_version_label`) | char |  |  |
| **Additional details** (`custom_field_text`) | char |  | Search across synchronized Paperless custom-field values. |
| **Custom Fields Json** (`custom_fields_json`) | text |  |  |
| **Deletion Approved At** (`deletion_approved_at`) | datetime |  |  |
| **Deletion Approved By** (`deletion_approved_by_id`) | many2one → `res.users` |  |  |
| **Deletion Reason** (`deletion_reason`) | text |  |  |
| **Document Date** (`document_date`) | date |  |  |
| **Document type** (`document_type_id`) | many2one → `usl.paperless.document.type` |  |  |
| **Document Type Name** (`document_type_name`) | char |  |  |
| **Has Distinct Archive File** (`has_distinct_archive_file`) | boolean |  |  |
| **Linked to Odoo** (`has_linked_record`) | boolean |  |  |
| **Intake Role** (`intake_role`) | selection | yes | Odoo presentation role captured when the Paperless root entered Documents. Business relationships carry their own role. Values: **Evidence**, **Library**, **Background**. |
| **In my library** (`is_in_my_library`) | boolean |  | Documents uploads, accessible library relationships, and documents starred by the current user. |
| **Is Prominent** (`is_prominent`) | boolean |  | Prominent roots may appear in Home and My library. Background-only roots remain available from business context and archive search. |
| **Starred for me** (`is_starred`) | boolean |  | Private Odoo preference; it does not change Paperless metadata. |
| **Last Error** (`last_error`) | text |  |  |
| **Link Count** (`link_count`) | integer |  |  |
| **Link** (`link_ids`) | one2many → `usl.document.link` |  |  |
| **Employee** (`linked_employee_id`) | many2one → `hr.employee` |  |  |
| **Linked record** (`linked_record_ref`) | char |  |  |
| **Mapped Contact** (`mapped_contact_id`) | many2one → `res.partner` |  |  |
| **Metadata Hash** (`metadata_hash`) | char |  |  |
| **Mime Type** (`mime_type`) | char |  |  |
| **Name** (`name`) | char | yes |  |
| **Original creation** (`original_created_at`) | datetime |  | Creation timestamp recorded by the system that supplied the document. |
| **Original Filename** (`original_filename`) | char |  |  |
| **Original modification** (`original_modified_at`) | datetime |  | Last modification timestamp recorded by the system that supplied the document. |
| **Paperless Created** (`paperless_created`) | datetime |  |  |
| **Paperless ID** (`paperless_id`) | integer | yes |  |
| **Paperless Modified** (`paperless_modified`) | datetime |  |  |
| **Paperless Url** (`paperless_url`) | char |  |  |
| **Permanently Deleted At** (`permanently_deleted_at`) | datetime |  |  |
| **Permission Checked At** (`permission_checked_at`) | datetime |  |  |
| **Permission Sync Error** (`permission_sync_error`) | text |  |  |
| **Permission Sync State** (`permission_sync_state`) | selection | yes | Values: **Pending**, **Synchronized**, **Failed**. |
| **Users allowed by linked records** (`permitted_user_ids`) | many2many → `res.users` |  |  |
| **Recently opened by me** (`recently_opened`) | boolean |  |  |
| **Retention hold** (`retention_hold`) | boolean |  | Blocks permanent deletion regardless of the Trash retention date. |
| **Retention Until** (`retention_until`) | datetime |  |  |
| **Review State** (`review_state`) | selection | yes | Values: **Needs attention**, **Ready for review**, **Reviewed**. |
| **Meaning (Semantic)** (`semantic_text`) | char |  | Search by meaning with the local BGE-M3 semantic index. |
| **Source** (`source`) | selection | yes | Values: **Uploaded from Odoo**, **Archived Odoo attachment**, **Odoo-generated authoritative output**, **External Paperless ingestion**. |
| **Status Sort Key** (`status_sort_key`) | char |  |  |
| **Submitted At** (`submitted_at`) | datetime |  |  |
| **Submitted By** (`submitted_by_id`) | many2one → `res.users` |  |  |
| **Tags** (`tag_ids`) | many2many → `usl.paperless.tag` |  |  |
| **Tag Sort Key** (`tag_sort_key`) | char |  |  |
| **Trashed At** (`trashed_at`) | datetime |  |  |
| **Moved to Trash by** (`trashed_by_id`) | many2one → `res.users` |  |  |
| **Trash source** (`trashed_by_label`) | char |  | The Odoo user is recorded for actions made in Odoo. Paperless 3.0's Trash API does not identify the user for actions made directly there. |
| **Version Count** (`version_count`) | integer |  |  |
| **File versions** (`version_ids`) | one2many → `usl.document.version` |  |  |

## Stable Business Entity Document Tag

`usl.document.context.tag`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Company** (`company_id`) | many2one → `res.company` | yes |  |
| **Namespace** (`namespace`) | char | yes |  |
| **Res** (`res_id`) | integer | yes |  |
| **Res Model** (`res_model`) | char | yes |  |
| **Tag** (`tag_id`) | many2one → `usl.paperless.tag` | yes |  |
| **Tag Name** (`tag_name`) | char | yes |  |

## Short-lived Document Download Grant

`usl.document.download.grant`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Allowed Company Ids Json** (`allowed_company_ids_json`) | json | yes |  |
| **Checksum** (`checksum`) | char |  |  |
| **Company** (`company_id`) | many2one → `res.company` |  |  |
| **Company Odoo** (`company_odoo_id`) | integer |  |  |
| **Current Company** (`current_company_id`) | integer | yes |  |
| **Database Name** (`database_name`) | char | yes |  |
| **Denial Count** (`denial_count`) | integer |  |  |
| **Document** (`document_id`) | many2one → `usl.document` |  |  |
| **Document Name** (`document_name`) | char | yes |  |
| **Document Odoo** (`document_odoo_id`) | integer | yes |  |
| **Document Version** (`document_version_id`) | many2one → `usl.document.version` |  |  |
| **Document Version Odoo** (`document_version_odoo_id`) | integer | yes |  |
| **Expires At** (`expires_at`) | datetime | yes |  |
| **Filename** (`filename`) | char | yes |  |
| **First Redeemed At** (`first_redeemed_at`) | datetime |  |  |
| **Issued At** (`issued_at`) | datetime | yes |  |
| **Issued By** (`issued_by_id`) | many2one → `res.users` |  |  |
| **Issued By Odoo** (`issued_by_odoo_id`) | integer | yes |  |
| **Last Denial Code** (`last_denial_code`) | char |  |  |
| **Last Denied At** (`last_denied_at`) | datetime |  |  |
| **Last Redeemed At** (`last_redeemed_at`) | datetime |  |  |
| **Mime Type** (`mime_type`) | char | yes |  |
| **Operation** (`operation`) | selection | yes | Values: **Download**. |
| **Paperless Document** (`paperless_document_id`) | integer | yes |  |
| **Paperless Version** (`paperless_version_id`) | char | yes |  |
| **Public** (`public_id`) | char | yes |  |
| **Redemption Count** (`redemption_count`) | integer |  |  |
| **Revocation Reason** (`revocation_reason`) | char |  |  |
| **Revoked At** (`revoked_at`) | datetime |  |  |
| **Revoked By** (`revoked_by_id`) | many2one → `res.users` |  |  |
| **Size Bytes** (`size_bytes`) | integer |  |  |
| **Token Hash** (`token_hash`) | char | yes |  |
| **Variant** (`variant`) | selection | yes | Values: **Original**, **Archive**. |

## Document Download Grant Audit Event

`usl.document.download.grant.audit`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Actor** (`actor_id`) | many2one → `res.users` |  |  |
| **Correlation** (`correlation_id`) | char |  |  |
| **Denial Code** (`denial_code`) | char |  |  |
| **Document Odoo** (`document_odoo_id`) | integer | yes |  |
| **Document Version Odoo** (`document_version_odoo_id`) | integer | yes |  |
| **Event Type** (`event_type`) | selection | yes | Values: **Issued**, **Redeemed**, **Revoked**, **Denied: expired**, **Denied: revoked**, **Denied: inactive user**, **Denied: company access**, **Denied: authorization**, **Denied: file unavailable**. |
| **Grant** (`grant_id`) | many2one → `usl.document.download.grant` | yes |  |
| **Grant Public** (`grant_public_id`) | char | yes |  |
| **Issued By Odoo** (`issued_by_odoo_id`) | integer |  |  |
| **Occurred At** (`occurred_at`) | datetime | yes |  |
| **Paperless Version** (`paperless_version_id`) | char | yes |  |
| **Variant** (`variant`) | selection | yes | Values: **Original**, **Archive**. |

## Archived Document Business Relationship

`usl.document.link`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Archive Mode** (`archive_mode`) | selection | yes | Values: **Mandatory retention**, **Automatic**, **Keep on request**, **Excluded**. |
| **Attachment Origin** (`attachment_origin`) | selection | yes | Values: **Documents workspace**, **Direct record upload**, **Chatter**, **Portal**, **Generated final output**, **Generated transient output**, **External Paperless intake**, **Migration**, **Backfill**. |
| **Company** (`company_id`) | many2one → `res.company` | yes |  |
| **Document** (`document_id`) | many2one → `usl.document` | yes |  |
| **Document Role** (`document_role`) | selection | yes | Current Odoo presentation role. Promotion or demotion changes only this relationship and never uploads another Paperless file. Values: **Evidence**, **Library**, **Background**. |
| **Linked At** (`linked_at`) | datetime | yes |  |
| **Linked By** (`linked_by_id`) | many2one → `res.users` | yes |  |
| **Policy Reason** (`policy_reason`) | char | yes |  |
| **Policy Role** (`policy_role`) | selection | yes | Document role resolved by the business archive policy. Values: **Evidence**, **Library**, **Background**. |
| **Record Name** (`record_name`) | char | yes |  |
| **Res** (`res_id`) | integer | yes |  |
| **Res Model** (`res_model`) | char | yes |  |
| **Version** (`version_id`) | char |  | Paperless version that supports this business record, when legally relevant. |

## Archived Document Link Mixin

`usl.document.link.mixin`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archived Documents** (`archived_document_count`) | integer |  |  |
| **Document archive issues** (`document_archive_failure_count`) | integer |  |  |
| **Documents processing** (`document_archive_pending_count`) | integer |  |  |

## Document Ingestion Operation

`usl.document.operation`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Access Scope** (`access_scope`) | selection |  | Values: **Company policy**, **Linked record access**. |
| **Accounting Evidence** (`accounting_evidence`) | boolean |  |  |
| **Acknowledged** (`acknowledged`) | boolean |  |  |
| **Acknowledged At** (`acknowledged_at`) | datetime |  |  |
| **Archive Mode** (`archive_mode`) | selection | yes | Values: **Mandatory retention**, **Automatic**, **Keep on request**, **Excluded**. |
| **Attachment Origin** (`attachment_origin`) | selection | yes | Values: **Documents workspace**, **Direct record upload**, **Chatter**, **Portal**, **Generated final output**, **Generated transient output**, **External Paperless intake**, **Migration**, **Backfill**. |
| **Attempt Count** (`attempt_count`) | integer |  |  |
| **Checksum** (`checksum`) | char | yes |  |
| **Company** (`company_id`) | many2one → `res.company` | yes |  |
| **Confidentiality** (`confidentiality`) | selection | yes | Odoo access policy captured when the ingestion request was created. Values: **Internal**, **Accounting evidence**, **HR restricted**, **Creator private**. |
| **Context Json** (`context_json`) | json |  |  |
| **Document** (`document_id`) | many2one → `usl.document` |  |  |
| **Document Role** (`document_role`) | selection | yes | Values: **Evidence**, **Library**, **Background**. |
| **Error Message** (`error_message`) | text |  |  |
| **Metadata Hash** (`metadata_hash`) | char |  |  |
| **Mime Type** (`mime_type`) | char |  |  |
| **Name** (`name`) | char | yes |  |
| **Next Attempt At** (`next_attempt_at`) | datetime |  |  |
| **Original Created At** (`original_created_at`) | datetime |  |  |
| **Original Modified At** (`original_modified_at`) | datetime |  |  |
| **Paperless Task** (`paperless_task_id`) | char |  |  |
| **Policy Reason** (`policy_reason`) | char | yes |  |
| **Processing Started At** (`processing_started_at`) | datetime |  |  |
| **Res** (`res_id`) | integer |  |  |
| **Res Model** (`res_model`) | char |  |  |
| **Retry Count** (`retry_count`) | integer |  |  |
| **Retry Of** (`retry_of_id`) | many2one → `usl.document.operation` |  |  |
| **Review Reason** (`review_reason`) | selection |  | Values: **Matching archive document is in Trash**, **Source business record was deleted**. |
| **Source** (`source`) | selection |  | Values: **Uploaded from Odoo**, **Archived Odoo attachment**, **Odoo-generated authoritative output**. |
| **Source Attachment Checksum** (`source_attachment_checksum`) | char |  |  |
| **Source Attachment** (`source_attachment_id`) | many2one → `ir.attachment` |  |  |
| **State** (`state`) | selection | yes | Values: **Pending**, **Uploading**, **Processing**, **Archived**, **Duplicate reused**, **Failed**. |
| **Replacement root document** (`target_document_id`) | many2one → `usl.document` |  |  |
| **User** (`user_id`) | many2one → `res.users` | yes |  |

## Documents One-click Shortcut

`usl.document.quick.filter`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Filters** (`filter_domain`) | text |  | The native Odoo filter conditions applied by this shortcut. |
| **First grouping** (`group_by_1`) | selection |  | Values: **Company**, **Correspondent**, **Document type**, **Employee**, **Privacy**, **Review status**, **Document month**, **Added month**. |
| **Second grouping** (`group_by_2`) | selection |  | Values: **Company**, **Correspondent**, **Document type**, **Employee**, **Privacy**, **Review status**, **Document month**, **Added month**. |
| **Third grouping** (`group_by_3`) | selection |  | Values: **Company**, **Correspondent**, **Document type**, **Employee**, **Privacy**, **Review status**, **Document month**, **Added month**. |
| **Icon** (`icon`) | char |  |  |
| **Saved search** (`ir_filter_id`) | many2one → `ir.filters` |  | Native Odoo search definition containing the shortcut domain, grouping, and ordering. |
| **Key** (`key`) | char | yes |  |
| **Name** (`name`) | char | yes |  |
| **Sequence** (`sequence`) | integer |  |  |
| **Available in Smart Views** (`smart_view_ids`) | many2many → `usl.document.smart.view` |  | Smart Views that show this optional shortcut below the native Odoo search bar. |
| **Sort first by** (`sort_by_1`) | selection |  | Values: **Document**, **Document date**, **Added date**, **Correspondent**, **Document type**, **Company**, **Tags**, **Status**. |
| **Sort second by** (`sort_by_2`) | selection |  | Values: **Document**, **Document date**, **Added date**, **Correspondent**, **Document type**, **Company**, **Tags**, **Status**. |
| **Sort third by** (`sort_by_3`) | selection |  | Values: **Document**, **Document date**, **Added date**, **Correspondent**, **Document type**, **Company**, **Tags**, **Status**. |
| **First direction** (`sort_direction_1`) | selection |  | Values: **Ascending**, **Descending**. |
| **Second direction** (`sort_direction_2`) | selection |  | Values: **Ascending**, **Descending**. |
| **Third direction** (`sort_direction_3`) | selection |  | Values: **Ascending**, **Descending**. |

## Documents Smart View

`usl.document.smart.view`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Available in Paperless** (`archive_native`) | boolean |  | Synchronize this shared view with Paperless. Odoo-only company, confidentiality, and business-record filters remain enforced only in Odoo. |
| **Correspondents** (`correspondent_ids`) | many2many → `usl.paperless.correspondent` |  |  |
| **Days** (`days`) | integer |  |  |
| **Document types** (`document_type_ids`) | many2many → `usl.paperless.document.type` |  |  |
| **Filter Json** (`filter_json`) | text |  |  |
| **Visible to groups** (`group_ids`) | many2many → `res.groups` |  | Leave empty for every Documents user. |
| **Icon** (`icon`) | char |  |  |
| **Key** (`key`) | char |  |  |
| **Name** (`name`) | char | yes |  |
| **Paperless Filter Json** (`paperless_filter_json`) | text |  |  |
| **Paperless Saved View ID** (`paperless_id`) | integer |  |  |
| **Paperless Sync Error** (`paperless_sync_error`) | text |  |  |
| **Paperless Sync State** (`paperless_sync_state`) | selection | yes | Values: **Pending**, **Synchronized**, **Needs attention**. |
| **Paperless Synced At** (`paperless_synced_at`) | datetime |  |  |
| **One-click shortcuts** (`quick_filter_ids`) | many2many → `usl.document.quick.filter` |  | Useful filters shown below the search bar for this view. They compose with normal search facets and tag shortcuts. |
| **Scope** (`scope`) | selection | yes | Values: **Shared**, **Personal**. |
| **Sequence** (`sequence`) | integer |  |  |
| **System Rule** (`system_rule`) | selection | yes | Values: **All accessible documents**, **Home**, **My library**, **Needs attention**, **Recently added**, **Accounting evidence**, **Projects**, **HR restricted**, **Inbox / To classify**, **Archive search**, **Trash**, **Paperless metadata**, **Saved filters**. |
| **Tags** (`tag_ids`) | many2many → `usl.paperless.tag` |  |  |
| **User** (`user_id`) | many2one → `res.users` |  |  |

## Private Documents Workspace State

`usl.document.user.state`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Document** (`document_id`) | many2one → `usl.document` | yes |  |
| **Last Opened At** (`last_opened_at`) | datetime |  |  |
| **Starred** (`starred`) | boolean |  |  |
| **User** (`user_id`) | many2one → `res.users` | yes |  |

## Paperless Document File Version

`usl.document.version`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Archive Checksum** (`archive_checksum`) | char |  |  |
| **Checksum** (`checksum`) | char |  |  |
| **Created At** (`created_at`) | datetime |  |  |
| **Document** (`document_id`) | many2one → `usl.document` | yes |  |
| **Has Distinct Archive File** (`has_distinct_archive_file`) | boolean |  |  |
| **Is Current** (`is_current`) | boolean |  |  |
| **Is Received Original** (`is_received_original`) | boolean |  |  |
| **Label** (`label`) | char | yes |  |
| **Metadata Hash** (`metadata_hash`) | char |  |  |
| **Mime Type** (`mime_type`) | char |  |  |
| **Original Created At** (`original_created_at`) | datetime |  |  |
| **Original Filename** (`original_filename`) | char |  |  |
| **Original Modified At** (`original_modified_at`) | datetime |  |  |
| **Page Count** (`page_count`) | integer |  |  |
| **Paperless Version** (`paperless_version_id`) | char | yes |  |
| **Source** (`source`) | selection |  | Values: **Uploaded from Odoo**, **Archived Odoo attachment**, **Odoo-generated authoritative output**, **External Paperless ingestion**. |
| **Submitted At** (`submitted_at`) | datetime |  |  |
| **Submitted By** (`submitted_by_id`) | many2one → `res.users` |  |  |

## Paperless Correspondent

`usl.paperless.correspondent`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Documents** (`accessible_document_count`) | integer |  | Documents carrying this metadata that the current Odoo user may access. |
| **Active** (`active`) | boolean |  |  |
| **Document Count** (`document_count`) | integer |  |  |
| **Ignore letter case** (`is_insensitive`) | boolean |  | Match upper- and lower-case text in the same way. |
| **Last Error** (`last_error`) | text |  |  |
| **Last Synced At** (`last_synced_at`) | datetime |  |  |
| **Words to look for** (`match`) | char |  | Text or pattern Paperless uses when automatically classifying documents. |
| **How documents match** (`matching_algorithm`) | selection | yes | Automatic learns from existing documents. The other options apply the matching pattern directly. Values: **None**, **Any word**, **All words**, **Exact match**, **Regular expression**, **Fuzzy word**, **Learn automatically**. |
| **Name** (`name`) | char | yes |  |
| **Paperless ID** (`paperless_id`) | integer | yes |  |
| **Mapped Contact** (`partner_id`) | many2one → `res.partner` |  | Optional business-identity mapping. It does not link documents or grant access. Paperless remains responsible for archive matching. |
| **Partner Mapping Hidden** (`partner_mapping_hidden`) | boolean |  | The correspondent is already mapped to a Contact outside the current user's accessible companies. |
| **Odoo Contact** (`partner_visible_id`) | many2one → `res.partner` |  | The mapped Contact, when it is accessible in the current user's companies. Hidden mappings never grant or reveal Contact access. |
| **Rejected suggestion** (`rejected_partner_id`) | many2one → `res.partner` |  |  |
| **Matching terms** (`rule_lines`) | text |  | For Any word or All words, enter one word or phrase per line. Odoo writes the equivalent supported Paperless match expression. |
| **Suggested Contact** (`suggested_partner_id`) | many2one → `res.partner` |  |  |

## Paperless Document Type

`usl.paperless.document.type`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Documents** (`accessible_document_count`) | integer |  | Documents carrying this metadata that the current Odoo user may access. |
| **Active** (`active`) | boolean |  |  |
| **Document Count** (`document_count`) | integer |  |  |
| **Ignore letter case** (`is_insensitive`) | boolean |  | Match upper- and lower-case text in the same way. |
| **Last Error** (`last_error`) | text |  |  |
| **Last Synced At** (`last_synced_at`) | datetime |  |  |
| **Words to look for** (`match`) | char |  | Text or pattern Paperless uses when automatically classifying documents. |
| **How documents match** (`matching_algorithm`) | selection | yes | Automatic learns from existing documents. The other options apply the matching pattern directly. Values: **None**, **Any word**, **All words**, **Exact match**, **Regular expression**, **Fuzzy word**, **Learn automatically**. |
| **Name** (`name`) | char | yes |  |
| **Paperless ID** (`paperless_id`) | integer | yes |  |
| **Matching terms** (`rule_lines`) | text |  | For Any word or All words, enter one word or phrase per line. Odoo writes the equivalent supported Paperless match expression. |

## Paperless Metadata Cache

`usl.paperless.metadata.mixin`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Documents** (`accessible_document_count`) | integer |  | Documents carrying this metadata that the current Odoo user may access. |
| **Active** (`active`) | boolean |  |  |
| **Document Count** (`document_count`) | integer |  |  |
| **Ignore letter case** (`is_insensitive`) | boolean |  | Match upper- and lower-case text in the same way. |
| **Last Error** (`last_error`) | text |  |  |
| **Last Synced At** (`last_synced_at`) | datetime |  |  |
| **Words to look for** (`match`) | char |  | Text or pattern Paperless uses when automatically classifying documents. |
| **How documents match** (`matching_algorithm`) | selection | yes | Automatic learns from existing documents. The other options apply the matching pattern directly. Values: **None**, **Any word**, **All words**, **Exact match**, **Regular expression**, **Fuzzy word**, **Learn automatically**. |
| **Name** (`name`) | char | yes |  |
| **Paperless ID** (`paperless_id`) | integer | yes |  |
| **Matching terms** (`rule_lines`) | text |  | For Any word or All words, enter one word or phrase per line. Odoo writes the equivalent supported Paperless match expression. |

## Paperless Tag

`usl.paperless.tag`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Documents** (`accessible_document_count`) | integer |  | Documents carrying this metadata that the current Odoo user may access. |
| **Active** (`active`) | boolean |  |  |
| **Child** (`child_ids`) | one2many → `usl.paperless.tag` |  |  |
| **Color** (`color`) | char |  |  |
| **Document Count** (`document_count`) | integer |  |  |
| **Add to new documents** (`is_inbox_tag`) | boolean |  | Paperless automatically adds inbox tags to newly received documents. |
| **Ignore letter case** (`is_insensitive`) | boolean |  | Match upper- and lower-case text in the same way. |
| **Last Error** (`last_error`) | text |  |  |
| **Last Synced At** (`last_synced_at`) | datetime |  |  |
| **Words to look for** (`match`) | char |  | Text or pattern Paperless uses when automatically classifying documents. |
| **How documents match** (`matching_algorithm`) | selection | yes | Automatic learns from existing documents. The other options apply the matching pattern directly. Values: **None**, **Any word**, **All words**, **Exact match**, **Regular expression**, **Fuzzy word**, **Learn automatically**. |
| **Name** (`name`) | char | yes |  |
| **Paperless ID** (`paperless_id`) | integer | yes |  |
| **Parent tag** (`parent_id`) | many2one → `usl.paperless.tag` |  |  |
| **Matching terms** (`rule_lines`) | text |  | For Any word or All words, enter one word or phrase per line. Odoo writes the equivalent supported Paperless match expression. |
| **Text Color** (`text_color`) | char |  |  |

## Odoo to Paperless Individual Identity

`usl.paperless.user.mapping`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Last Error** (`last_error`) | text |  |  |
| **Last Verified At** (`last_verified_at`) | datetime |  |  |
| **Pocket ID identity** (`oidc_identity_id`) | many2one → `usl.oidc.identity` |  | Immutable Pocket ID identity used by this Odoo user. Paperless still keeps its own numeric user identity for object permissions. |
| **Pocket identity** (`oidc_subject_fingerprint`) | char |  |  |
| **Paperless User** (`paperless_user_id`) | integer | yes |  |
| **Paperless Username** (`paperless_username`) | char | yes |  |
| **QA local login** (`qa_local_identity`) | boolean |  | Explicit exception for the isolated QA environment's documented username/admin test accounts. It is ignored everywhere else. |
| **Sync State** (`sync_state`) | selection | yes | Values: **Pending verification**, **Verified**, **Failed**. |
| **User** (`user_id`) | many2one → `res.users` | yes |  |
