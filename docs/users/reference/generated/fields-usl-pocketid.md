---
title: "Fields: usl_pocketid"
type: reference
description: Every field `usl_pocketid` defines or adds to Odoo, by model, with its label, type and meaning.
lang: en
persona: everyone
source:
  - custom-addons/usl_pocketid
generated: true
---

<!-- Generated by scripts/docs-reference from the product's registry. Change the code, then run `make docs-reference`. -->

# Fields: usl_pocketid

Every field `usl_pocketid` defines or adds to Odoo, by model, with its label, type and meaning.

## OAuth2 provider

`auth.oauth.provider`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Allow pre-approved unique email linking** (`usl_allow_unique_email_link`) | boolean |  | Allow a verified email to create an identity link only when exactly one active internal Odoo user was explicitly pre-approved. |
| **OIDC Logout Endpoint** (`usl_end_session_endpoint`) | char |  | Validated Pocket ID endpoint used for provider-aware logout. |
| **OIDC Issuer** (`usl_oidc_issuer`) | char |  | Exact issuer expected in Pocket ID ID tokens. |
| **USL Pocket ID provider** (`usl_pocketid`) | boolean |  | This provider is governed by the USL Pocket ID environment. |
| **Odoo Public Base URL** (`usl_public_base_url`) | char |  | Canonical public Odoo URL used to construct the registered callback. |
| **Required Pocket ID Group** (`usl_required_group`) | char |  | Pocket ID group required to authenticate. It never grants Odoo groups. |
| **Token endpoint authentication** (`usl_token_auth_method`) | selection |  | Values: **Client secret HTTP Basic**, **Client secret POST body**. |

## Employee

`hr.employee`, extended by this module (defined by `usl_documents`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Personal Sender Addresses** (`usl_sender_alias_ids`) | one2many → `usl.mail.sender.alias` |  |  |

## Contact

`res.partner`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Personal Sender Addresses** (`usl_sender_alias_ids`) | one2many → `usl.mail.sender.alias` |  |  |

## User

`res.users`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Sealed emergency administrator** (`usl_local_break_glass`) | boolean |  | Independent administrator accepted only on the audited emergency route during a short deployment-approved incident window. |
| **OIDC identities** (`usl_oidc_identity_ids`) | one2many → `usl.oidc.identity` |  |  |
| **Pocket ID login enabled** (`usl_pocketid_access`) | boolean |  | Allow an already governed Pocket ID identity to log in as this user. |
| **Pre-approve verified email link** (`usl_pocketid_email_link`) | boolean |  | Allow first-login linking only when the verified Pocket ID email uniquely and exactly matches this active governed user. |
| **Last Pocket ID sign-in** (`usl_pocketid_last_login_at`) | datetime |  |  |
| **Pocket ID email** (`usl_pocketid_security_email`) | char |  |  |
| **Pocket ID status** (`usl_pocketid_security_state`) | selection |  | Values: **Connected**, **Ready for first sign-in**, **Not enabled**. |
| **Personal Sender Addresses** (`usl_sender_alias_ids`) | one2many → `usl.mail.sender.alias` |  |  |
| **SSO-only login** (`usl_sso_only_login`) | boolean |  |  |

## Password Check Wizard

`res.users.identitycheck`, defined by this module. Transient: the record lives only while its dialog is open.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Auth Method** (`auth_method`) | selection |  | Values: **Password**, **Passkey**, **Pocket ID**. |

## Verified Personal Email Address

`usl.mail.sender.alias`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Email** (`email`) | char | yes |  |
| **Email Normalized** (`email_normalized`) | char |  |  |
| **Partner** (`partner_id`) | many2one → `res.partner` | yes |  |
| **State** (`state`) | selection | yes | Values: **Pending verification**, **Verified**. |
| **Verification Expires At** (`verification_expires_at`) | datetime |  |  |
| **Verification Sent At** (`verification_sent_at`) | datetime |  |  |
| **Verification Token Digest** (`verification_token_digest`) | char |  |  |
| **Verified At** (`verified_at`) | datetime |  |  |

## USL OIDC Audit Event

`usl.oidc.audit.event`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Actor** (`actor_id`) | many2one → `res.users` |  |  |
| **Event Type** (`event_type`) | selection | yes | Values: **Login succeeded**, **Login denied**, **Identity linked**, **Identity relinked**, **Identity enabled**, **Identity disabled**, **Configuration changed**, **User policy applied**. |
| **Identity** (`identity_id`) | many2one → `usl.oidc.identity` |  |  |
| **Occurred At** (`occurred_at`) | datetime | yes |  |
| **Provider** (`provider_id`) | many2one → `auth.oauth.provider` |  |  |
| **Reason Code** (`reason_code`) | char | yes |  |
| **Subject Fingerprint** (`subject_fingerprint`) | char |  |  |
| **User** (`user_id`) | many2one → `res.users` |  |  |

## USL OIDC Identity Link

`usl.oidc.identity`, extended by this module (defined by `usl_documents`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Active** (`active`) | boolean |  |  |
| **Issuer** (`issuer`) | char | yes |  |
| **Last Display Name** (`last_display_name`) | char |  |  |
| **Last Email** (`last_email`) | char |  |  |
| **Last Login At** (`last_login_at`) | datetime |  |  |
| **Link Method** (`link_method`) | selection | yes | Values: **Explicit administrator link**, **Pre-approved verified unique email**. |
| **Linked At** (`linked_at`) | datetime | yes |  |
| **Linked By** (`linked_by_id`) | many2one → `res.users` | yes |  |
| **Provider** (`provider_id`) | many2one → `auth.oauth.provider` | yes |  |
| **Subject** (`subject`) | char | yes |  |
| **Subject Fingerprint** (`subject_fingerprint`) | char |  |  |
| **User** (`user_id`) | many2one → `res.users` | yes |  |
