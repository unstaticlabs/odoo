---
title: "Fields: usl_access_control"
type: reference
description: Every field `usl_access_control` defines or adds to Odoo, by model, with its label, type and meaning.
lang: en
persona: everyone
source:
  - custom-addons/usl_access_control
generated: true
---

<!-- Generated by scripts/docs-reference from the product's registry. Change the code, then run `make docs-reference`. -->

# Fields: usl_access_control

Every field `usl_access_control` defines or adds to Odoo, by model, with its label, type and meaning.

## User

`res.users`, extended by this module (defined by `rebuild_account_migration`).

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Effective Distribution access** (`usl_access_summary`) | char |  |  |
| **Irreversible Actions** (`usl_has_irreversible_actions`) | boolean |  |  |
| **Identity classification** (`usl_identity_classification`) | selection |  | Values: **Active Pocket ID-managed user**, **Historical identity; login disabled**, **Portal or external identity**, **Obsolete or duplicate; decision required**, **Sealed emergency administrator**, **Autonomous Agent**. |
| **AI Agent** (`usl_is_ai_agent`) | boolean |  |  |
| **Managed Agent identity** (`usl_managed_agent_id`) | many2one → `usl.agent` |  |  |
| **Usl Owned Agent Count** (`usl_owned_agent_count`) | integer |  |  |
| **Agents** (`usl_owned_agent_ids`) | one2many → `usl.agent` |  |  |

## Autonomous Agent

`usl.agent`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Access Mode** (`access_mode`) | selection | yes | Summarizes whether delegated applications are read-only, read/write, or mixed. Values: **Read-only**, **Read/write**, **Mixed**. |
| **Access Summary** (`access_summary`) | char |  |  |
| **Active Credential Count** (`active_credential_count`) | integer |  |  |
| **Approved effective access** (`approved_effective_group_ids`) | many2many → `res.groups` |  |  |
| **Activity** (`audit_event_ids`) | one2many → `usl.audit.event` |  |  |
| **Authority Reduced At** (`authority_reduced_at`) | datetime |  |  |
| **Authority Reduction Reason** (`authority_reduction_reason`) | char |  |  |
| **Can Admin Agents** (`can_admin_agents`) | boolean |  |  |
| **Default company** (`company_id`) | many2one → `res.company` | yes |  |
| **Companies** (`company_ids`) | many2many → `res.company` | yes |  |
| **Credential Count** (`credential_count`) | integer |  |  |
| **API keys** (`credential_ids`) | one2many → `usl.agent.credential` |  |  |
| **Application access** (`delegated_group_ids`) | many2many → `res.groups` |  |  |
| **May record receipt decisions** (`expense_receipt_waiver_authority`) | boolean |  | Continuing an expense without a receipt is a documented decision rather than ordinary data entry, so an Agent cannot take it on its own. Enabling this is the owner's explicit acceptance that this Agent may record the decision on their authority. The Agent still has to preview the decision and confirm it in a second call, and the owner stays accountable for what it records. |
| **Last Used At** (`last_used_at`) | datetime |  |  |
| **Name** (`name`) | char | yes |  |
| **Needs Attention** (`needs_attention`) | boolean |  |  |
| **Owner companies** (`owner_company_ids`) | many2many → `res.company` |  |  |
| **Owner** (`owner_id`) | many2one → `res.users` | yes |  |
| **Purpose** (`purpose`) | text | yes |  |
| **Read-only application access** (`read_only_group_ids`) | many2many → `res.groups` |  | Explicit delegated application roles whose mutations are blocked by Agent policy. |
| **Read Profile Update Available** (`read_profile_update_available`) | boolean |  |  |
| **Settings** (`settings_access`) | selection |  | Values: **No access**, **Read-only**, **Read/write**. |
| **State** (`state`) | selection | yes | Values: **Active**, **Suspended**. |
| **Suspension Reason** (`suspension_reason`) | char |  |  |
| **Agent identity** (`user_id`) | many2one → `res.users` | yes |  |
| **Available application access** (`view_group_hierarchy`) | json |  |  |

## Agent API Credential

`usl.agent.credential`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Agent** (`agent_id`) | many2one → `usl.agent` | yes |  |
| **Expiration Date** (`expiration_date`) | datetime | yes |  |
| **Last Used At** (`last_used_at`) | datetime |  |  |
| **Name** (`name`) | char | yes |  |
| **Native Key** (`native_key_id`) | integer | yes |  |
| **Revoked At** (`revoked_at`) | datetime |  |  |
| **Revoked By** (`revoked_by_id`) | many2one → `res.users` |  |  |
| **Status** (`status`) | selection |  | Values: **Active**, **Expired**, **Revoked**. |

## Create Agent API Key

`usl.agent.key.wizard`, defined by this module. Transient: the record lives only while its dialog is open.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Agent** (`agent_id`) | many2one → `usl.agent` | yes |  |
| **Duration** (`duration`) | selection | yes | Values: **90 days**, **1 year**, **5 years**, **Custom date**. |
| **Expiration Date** (`expiration_date`) | datetime |  |  |
| **Name** (`name`) | char | yes |  |

## Transfer Agent Ownership

`usl.agent.transfer.wizard`, defined by this module. Transient: the record lives only while its dialog is open.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Agent** (`agent_id`) | many2one → `usl.agent` | yes |  |
| **Consequence** (`consequence`) | text |  |  |
| **New Owner** (`new_owner_id`) | many2one → `res.users` | yes |  |

## USL Distribution Audit Event

`usl.audit.event`, defined by this module.

false

| Field | Type | Required | Meaning |
| --- | --- | --- | --- |
| **Action Key** (`action_key`) | char |  |  |
| **Action Name** (`action_name`) | char | yes |  |
| **Actor** (`actor_id`) | many2one → `res.users` | yes |  |
| **Actor Is Agent** (`actor_is_agent`) | boolean | yes |  |
| **Agent** (`agent_id`) | many2one → `usl.agent` |  |  |
| **Changes Json** (`changes_json`) | text |  |  |
| **Company** (`company_id`) | many2one → `res.company` |  |  |
| **Correlation** (`correlation_id`) | char |  |  |
| **Credential** (`credential_id`) | many2one → `usl.agent.credential` |  |  |
| **Event Type** (`event_type`) | selection | yes | Values: **Agent mutation**, **Protected action**, **Agent API call**. |
| **Model Name** (`model_name`) | char | yes |  |
| **Occurred At** (`occurred_at`) | datetime | yes |  |
| **Operation** (`operation`) | selection | yes | Values: **Create**, **Update**, **Delete**, **Action**, **Read**, **Call**. |
| **Origin** (`origin`) | char | yes |  |
| **Outcome** (`outcome`) | selection | yes | Values: **Succeeded**, **Denied**, **Failed**. |
| **Owner** (`owner_id`) | many2one → `res.users` |  |  |
| **Policy Digest** (`policy_digest`) | char |  |  |
| **Record Count** (`record_count`) | integer |  |  |
| **Record** (`record_ids`) | text |  |  |
| **Remote Address** (`remote_address`) | char |  |  |
| **Request** (`request_id`) | char |  |  |
| **User Agent** (`user_agent`) | char |  |  |
