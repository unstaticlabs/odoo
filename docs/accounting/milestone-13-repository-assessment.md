# Milestone 13 repository assessment

Generated for the accounting-core parity implementation slice and updated after the 2026-07-23 future reference-rate rehearsal.

## Trusted existing work

- The repository is an Odoo 19 Community fork with upstream addons in `addons/` and Odoo core in `odoo/`.
- Local custom work is isolated under `custom-addons/`.
- `custom-addons/usl_bootstrap` creates a development-safe French accounting baseline with synthetic `.test` data.
- Docker Compose and the Dev Container share the same source tree and addon path strategy.
- Existing accounting docs define invariants, migration gates, parity evidence and product boundaries.
- The accounting compatibility harness restores the Odoo Online source dump into an isolated PostgreSQL service and resets only the disposable target database.
- `custom-addons/rebuild_account_migration` now imports posted source accounting through the target ORM with source traces on companies, partners, accounts, journals, taxes, moves, move lines, move-backed payments, non-posted source move workflow review records, source move-line workflow review records, source payment workflow review records without journal entries, cross-boundary source reconciliation review records, source accounting report catalogue records, bank statement lines, analytic plans/accounts/lines, reconciliations, attachments and imported assets.
- The latest clean rehearsal validates benchmark ledger parity for Unstatic Labs from `2024-01-10` through `2025-09-30` and preserves USL lock dates.
- Future rate automation now uses a focused official ECB adapter over native `res.currency.rate` records because the checked Community/OCA 19 dependency set has no deployable updater. The daily cron is active, source-traced historical rows are never overwritten, and `make accounting-currency-rate-provider` proves a live idempotent retrieval. The latest run stored GBP `0.8534` and USD `1.1408` per EUR for `2026-07-22`, created no duplicate on rerun and preserved all `1,877` source-traced rates. Manager access and reviewer denial also passed in the browser.
- The isolated Track B database reconstructs all `325` source expenses dated `2025-10-01` through `2026-06-30` through native `hr.expense` submission, approval/refusal, receipt and company-payment workflows. It validates all expense fields and `176` generated accounting moves with `0` blocked/mismatched cases, creates `97` company payments and `79` grouped employee receipts, then creates `106` native bank transactions and replays `181` source expense-settlement edges through OCA. All `97` company payments and `95` employee-paid expenses finish paid. Expense settlement preserves `19` exact outside-only bank counterparts. The following document-settlement stage covers all `233` directly linked bank transactions and `339` source document/bank edges, creating `225` additional bank lines and `256` additional partials after reusing the `8` bank lines and `83` edges shared with expense settlement; all company/transaction-currency partials, endpoints and `241` due-line residuals match, while `48` more outside-only counterparts are preserved exactly. General Reconciliation then covers all `111` non-bank document partials and `114` document endpoints by posting `21` native manual entries with `72` lines, creating `71` manual/netting partials and tracing `40` native exchange partials. Direct bank categorization adds `1,415` bank transactions, applies `1,229` OCA categorization transitions, preserves `186` source-open transactions and validates all five analytic allocations. The external stage completes the final `95` bank transactions with `125` exact counterpart lines, `17` manual endpoint moves, `75` native input partials and `12` native exchange partials. Native coverage is `1,841/1,841`; only `48` draft/post-cutoff relationships remain explicitly classified rather than forced into invalid reconciliation states.
- A third disposable `odoo_rebuild_accounting_replacement` database now proves the hybrid architecture. It clones only a completed Track B state and exact-imports the benchmark history while reusing four validated native move representations. Historical parity is exact at `2,046` moves, `4,809` lines and debit/credit `1,064,045.02`; the combined candidate has `4,541` posted moves, `10,727` posted lines, no unbalanced move and no duplicate source identity. Every current-period journal and account-balance difference is classified; the `12` account differences net to EUR `0.00`, and the remaining EUR `2.64` profit-and-loss difference is attributed to native exchange timing. Replacement report/role/browser validation now passes for both manager and single-company reviewer, including `105` Trial Balance rows, `245` vendor documents and `325` expenses. Reviewer expense access preserves Odoo's public employee boundary, hides operational mutation controls and denies create/write/unlink at the server. Professional acceptance and promotion remain open.
- Historical sequence and chronology validation now blocks on exact source/target entry reference, date, journal, sequence prefix and sequence number. The four reused native aliases are normalized through Odoo's ORM to their exact source `OD` references; retaining generated native names was rejected as silent historical renumbering, and duplicate moves were rejected because they would duplicate accounting effects. The locked `2,046`-move benchmark source and replacement both have zero blank or duplicate entry references, zero duplicate sequence-number groups, `2` sequence gaps and `3` date-order decreases. The broader exact replay through the `2026-07-21` snapshot also matches source exactly across `4,843` moves, with zero blank/duplicate references or sequence numbers, `16` sequence gaps and `104` date-order decreases. These are preserved source anomalies, surfaced as one P2 accountant-owned discrepancy, and still require a professional explanation or acceptance.
- FEC role validation now passes on the hybrid candidate for Accounting Manager, scoped accountant reviewer and finance operator. All three generated and downloaded the `4,781`-row benchmark with balanced debit/credit `1,064,045.02`. Reviewer/operator exports are locked to a complete posted test FEC with no journal exclusions; only the manager may use the official path that can advance lock dates. The Download tab is independent of preview state, and the custom wrapper consumes Odoo's lazy native stream on the request cursor so the official transient remains visible.
- Accounting report visibility now exists through read-only Odoo report views for trial balance, general ledger, journal report, partner ledger, open items, aged balances, financial statements, French annual statements, VAT/tax lines, EC Sales List, OSS Sales, OSS Imports, historical bank reconciliation, currency gain/loss and exposure, cash-flow summary, executive summary, analytic distribution, fixed assets, imported depreciation schedule and imported deferred schedule. Sampled Odoo action checks prove these imported report views can drill down to contributing journal items where journal-item drill-down is applicable, and to `account.analytic.line` for the analytic report.
- The French annual-statement view now computes USL benchmark Bilan Actif, Bilan Passif, Compte de résultat, SIG and CAF lines from imported ledger account mappings, and harness checks match the supplied benchmark anchors for total assets, passif, current-year result, turnover, operating result, current result before tax, net result, value added, EBE and CAF.
- The asset replay now preserves three source assets and 91 source asset depreciation schedule rows reconstructed from asset-linked source depreciation moves; the latest clean rehearsal compares source and target schedule rows with no missing, extra or mismatched rows.
- The deferred schedule replay now preserves all 110 source `account_move_deferred_rel` rows as review records: 37 linked to imported posted entries and 73 source draft forecast rows. No deferred-revenue rows are present in the current source corpus.
- The EC/OSS report slice now exposes the active source EC Sales List, OSS Sales and OSS Imports reports as ledger-derived review views and CSV/XLSX/PDF export workflows. Current-period EC Sales List evidence contains 4 rows for country `ES` and taxable amount `37,555.12`; OSS Sales and OSS Imports are explicit empty exports for the current source corpus.
- The first French tax-package mapping view now exposes 31 ledger/evidence-derived 2065-SD, 2033-A/B/C/D and 3517-S-SD/CA12 review lines through Odoo, including drill-down where journal-item evidence is applicable and CSV/XLSX/PDF export checks through the imported report export wizard.
- The report harness now includes user-facing bank reconciliation, currency gain/loss/exposure and analytic-distribution report checks. The benchmark bank reconciliation report contains `1,164` imported statement lines totaling `56,170.11` with zero residual. The benchmark currency report contains `14` grouped rows across foreign-currency ledger and realized exchange gain/loss sections. The closed benchmark has no source analytic lines, so the benchmark analytic report is explicitly empty; the current-period analytic report contains `53` grouped rows over `632` imported source analytic lines, allocated balance `101,481.23`, and drill-down to contributing analytic lines.
- The importer now represents all `13` source `account.payment` records without source journal entries as review-only `rebuild.account.payment.review` records. The clean rehearsal compares all `13` source rows to all `13` target review records with no missing, extra or mismatched rows, while preserving that they have no ledger debit or credit effect.
- The importer now represents all `194` non-posted source `account.move` records as review-only `rebuild.account.move.review` records. The clean rehearsal compares all `194` source rows to all `194` target review records with no missing, extra or mismatched rows, while preserving that they have no posted ledger effect. The Odoo review form now exposes a source-line smart button that opens the preserved `rebuild.account.move.line.review` rows for the selected source move.
- The importer now derives `194` `rebuild.account.document.regeneration.case` records from those non-posted source moves as an audited workbench for isolated document-regeneration mode. Target validation compares all `194` source-derived cases to all `194` target cases with no missing, extra or mismatched rows. Current classification is `189` ready candidates and `5` explicit review-only/not-applicable cases: `2` cancelled source records and `3` zero-line draft records without accounting effect. `make accounting-document-regeneration` now validates all `189` candidate-ready cases in an isolated native draft-generation pass: `37` draft business documents and `152` draft journal entries are generated or reused as target `account.move` drafts, their line counts and debit/credit totals match the preserved source line reviews, and `0` generated drafts are posted or included in posted replay controls. The latest clean target has `0` blocked document-regeneration cases; the previous P2 document-regeneration discrepancy is resolved and the `5` not-applicable cases remain visible in the workbench as review evidence.
- The importer now represents all `467` source move-line workflow rows as review-only `rebuild.account.move.line.review` records: `466` non-posted source accounting lines and the single posted source display line without an account. The clean rehearsal compares all `467` source rows to all `467` target review rows with no missing, extra or mismatched rows, while preserving that they have no posted target journal-item effect.
- The importer now represents `75` cross-boundary source reconciliation relationships as review-only `rebuild.account.reconciliation.review` records: `39` partial reconciliations and `36` full reconciliations that touch imported source journal items but also missing endpoints outside the selected posted replay boundary. The current missing-endpoint inventory is explicit: all `39` partial rows are posted-to-draft source relationships totaling `4,082.49`; the full-reconcile review rows contain `34` one-missing-draft cases totaling `2,141.81` and `2` two-missing-draft cases totaling `940.68`. `make accounting-document-regeneration` now links every review row to generated draft endpoint coverage: `77` missing source-line mentions resolve to generated target draft lines traced as `account.move.line.document_regeneration`. The queue defaults to all pending-policy rows rather than hiding represented rows, and the read-only form can open imported endpoints, generated endpoints, an exact partial pair or the complete full-reconciliation scope. Validation compares all `75` source rows to all `75` target review rows with no missing, extra or mismatched rows, including missing source move IDs, states and dates, while preserving that the missing endpoints are not silently reconciled in the native target graph.
- `make accounting-target-reconciliation-probe` now adds rollback-only native reconciliation evidence for the cross-boundary boundary. It samples a posted-to-draft source partial reconciliation, resolves the imported posted endpoint and generated draft endpoint, attempts native `account.partial.reconcile` creation inside an Odoo savepoint, forces rollback and verifies no native partial remains. The stage updates the cross-boundary discrepancy with the probe result while preserving that applying those native partials remains an accountant/product decision outside the exact posted replay baseline. The Odoo review form exposes a gated `Preview Native Partial` / `Record Decision` / manager-only `Apply Native Partial` workflow for partial rows and a `Preview Full Scope` / review-only-or-separate-workflow decision path for full rows. Permanent addon tests cover both previews, source-only endpoint resolution, authorization, missing-decision blocking and idempotent no-duplicate partial application. The manager/reviewer browser journey shows all `75` rows, balanced partial/full previews, no direct editing and no reviewer apply control; it leaves `2,534` native partials, `0` linked decisions and `0` applied boundary rows unchanged.
- The importer now represents all `38` active source `account.report` records as `rebuild.account.source.report` catalogue records and imports the source report structure as Odoo evidence: `702` report lines, `1,227` expressions and `141` columns. Validation and comparison now compare source and target report identity, names, country, root report, custom handler, filters, line/column/expression/external-value counts, expression engines, parity decision, target-equivalent status, target evidence key, line hierarchy, expression formulas and columns with no missing, extra or mismatched rows. Current classification is `23` mandatory parity, `10` operational parity, `3` accountant-requested and `2` removed-as-unused association reports; all `38` now have explicit target treatment and no active source report currently lacks an assigned target equivalent or scope decision. The report stage now updates post-export parity evidence: all `38` reports are Level 4 evidence partial and `0` have Level 4 accepted parity. The live P0 report-suite discrepancy is classified as `legal_or_accounting_uncertainty`, with target value `38 Level 4 technical evidence packages; 0 accountant-accepted reports; 0 missing target equivalents`, so the blocker reflects professional acceptance rather than an unimplemented report family.
- The importer now records the deductible VAT goods/services benchmark value as two explicit `rebuild.account.external.report.value` records for the 2033-D and CA12 review fields. The tax-package SQL view reads this benchmark from the external-value records instead of embedding it as a report constant and reconciles it to the posted source CA12 clearing entry where present. `artifacts/accounting-compat/private/vat-benchmark-investigation-2025-09-30.json` now proves that source and target account `445660` match exactly for the benchmark period (`253` lines, debit `3,014.09`, credit `3,014.09`, balance `0.00`) and that posted journal entry `OD000000009` on `2025-09-30` contains the source CA12 `445660` credit line for `1,960.00`. The Odoo tax-package rows for 2033-D and 3517-S now show amount `1,960.00`, benchmark amount `1,960.00`, ledger amount `1,960.00`, zero difference and no `EXTERNAL_VALUE_DIFFERENCE` classification, while preserving gross `445660` turnover `3,014.09` in the evidence text. The previous P1 VAT discrepancy is resolved.
- The imported accounting report export wizard now produces CSV, XLSX and PDF exports for the supported imported reports, plus an Odoo-backed FEC TXT export in manager-official or reviewer/operator test mode, with harness checks covering metadata, row counts, XLSX workbook structure and PDF file structure. The FEC dossier now also includes a local article A47 A-1 structural preflight and a DGFiP Test Compta Demat source-validation run. The current FEC file has the required first 18 BIC/IS fields in order, `4,781` data rows, `2,032` balanced journal/entry groups, `0` invalid row/date/amount/account findings, `0` chronology decreases, DGFiP source-validator exit code `0`, `0` blocking DGFiP log rows and validator PDF SHA-256 `9ef6f1a3a243359981c174f7a53fda9d2dd8796d63d2cc3da7001618ba12a339`.
- The accountant-requested grouping reports now have explicit target evidence. The tax grouping exports by account/tax and by tax/account each contain `6` benchmark rows with debit `9,168.27`, credit `5,726.27` and balance `3,442.00`; the fixed-asset account grouping contains `2` rows with gross value `10,430.49` and imported net value `8,754.44`. CSV/XLSX/PDF exports and preview source actions passed for all three.
- The addon now adds normal Accounting > Reporting launch actions for the mandatory report families. The current validation database contains `32` report-export wizard launch actions and `31` launcher menus under Odoo's standard legal statement, partner, management and tax/fiscal report groups; representative action contexts are covered by permanent addon tests. The launcher set includes explicit 2024 PCG variants for French Balance Sheet, French Profit and Loss, and SIG/CAF.
- The report wizard now supports in-Odoo preview before export through transient `rebuild.account.report.preview.line` rows. Each preview line has an `Open Sources` action that opens contributing `account.move.line` records, or `account.analytic.line` for analytic previews, using stable source IDs, source tax tags and account-code/prefix evidence where available. Harness checks validate preview metadata, row counts, truncation and source-action model/domain behavior for every supported non-FEC report; the latest sampled runtime evidence includes Trial Balance `68` rows, General Ledger `4,809` rows, French annual statements `36` rows, tax grouping by account/tax `6` rows, tax grouping by tax/account `6` rows, fixed assets by account `2` rows, explicit empty Customer Statement preview and explicit empty Aged Payable preview.
- `rebuild.account.review.summary` now provides a read-only Odoo review surface under Accounting > Review > Rebuild Evidence. It aggregates latest import status, imported posted-ledger totals, source-report coverage, source-report parity levels, open P0/P1 discrepancies, pending/recorded review-decision counts, external report-value counts, document-regeneration case counts and workflow-review record counts per imported company, with actions to open discrepancies, review decisions, external values, document-regeneration cases, imported journal items and report exports. The latest accountant-access harness validates the Unstatic Labs summary as `blocked`, with `1` open P0, `1` open P1, `45` active pending review decisions, `2` pending external report values, `194` document-regeneration cases, all `38` source reports at Level 4 evidence partial, `11,386` visible imported USL move lines, `1,053` workflow/review records and no visible USL Media journal items for the USL-only reviewer scope. The chronology P2 is included in the active decision queue.
- `rebuild.account.review.decision` now provides a durable Odoo acceptance surface for report parity, FEC validation, tax external values, discrepancy acceptance, deliberate scope exclusions and milestone closure gates. Discrepancies, source report catalogue records and external report values can prefill review decisions with source/target values, evidence keys, remaining risk and next actions. The reports stage seeds `45` active pending records: `38` linked source-report reviews, `3` linked open-discrepancy reviews, `2` linked external-value reviews, one FEC official-validation review and one Milestone 13 closure review. Marking a decision as recorded now requires a non-pending conclusion, propagates accepted conclusions to the linked source report, external value and discrepancy records, and makes recorded/superseded decisions immutable except through explicit supersession. The seeding harness preserves recorded decisions, updates only drafts and ignores superseded rows when a fresh pending review is needed; the refreshed exact target has no superseded review-decision rows. Permanent addon tests and the accountant-access harness prove that the accountant reviewer can create/update draft review decision notes and external report-value evidence while still being blocked from modifying imported posted moves or directly editing discrepancy records; the harness also runs a rollback-only recorded-decision propagation probe.
- `make accounting-readiness` now emits `readiness-assessment.json` and `readiness-assessment.md` as durable closure-control artifacts. The readiness assessment aggregates technical stage statuses, source/target identity, open discrepancy counts, review-decision queues, source-report coverage and the closure recommendation. Its final gate set includes hybrid reset/import/validation/browser evidence, FEC role-browser evidence and reconciliation review-browser evidence, and `make accounting-evidence` indexes the same artifacts. The hybrid validation's `partial` classification is the explicitly expected technical status for its fully classified native differences; it does not promote the candidate. The current assessment has `0` technical failures and remains `blocked` while open P0/P1 discrepancies or draft professional review decisions remain.
- The source-inspection `parity-matrix-v1.json` discovery baseline is now promoted by the report stage into final evidence-backed classifications. The current `54` rows contain `10` implemented capabilities, `39` technically complete but professionally partial capabilities/reports, `4` not-applicable rows and `1` explicit deferral, with `0` discovery rows and `0` technical gaps. Reports fail their technical gate if a discovery or technical-gap row remains.
- Recurring end-of-run discrepancies are now upserted idempotently. The current exact target has `3` open discrepancies (`1` P0, `1` P1 and `1` accountant-owned P2) plus resolved DGFiP FEC-validation, VAT CA12-clearing and document-regeneration scope discrepancies; the VAT CA12-clearing evidence now reconciles in a clean run and does not create a source-to-target discrepancy. Repeated import no longer inflates the open blocker count.
- The harness now includes a target import idempotence guardrail. `make accounting-target-idempotence` snapshots source-traced accounting consequence counts and debit/credit summaries, reruns the exact-ledger target import against an already processed disposable target, reruns target validation and fails if the signature changes. The importer now preserves post-generation document-regeneration state during repeated import, so the `189` validated candidate drafts stay validated, the `5` review-only cases stay not-applicable, and `0` document-regeneration blockers are reintroduced. The latest clean rehearsal passed with `signature_matches = true`, `observed_import_run_delta = 1`, `target_validate_status = passed` and no duplicate-trace failures; the one new import-run row is expected audit evidence, not a duplicated accounting consequence.
- The harness now includes rollback-only target conflict and invariant failure guardrails. `make accounting-target-failure-tests` injects a duplicate source-trace record in `rebuild.account.move.review` inside an Odoo savepoint, verifies duplicate detection, rolls back and verifies the target signature is unchanged. It also temporarily makes one imported posted move unbalanced, verifies the unbalanced-move detector catches it, rolls back and verifies clean state. The same stage injects invalid `account_move_line.account_id`, `account_move_line_account_tax_rel.account_tax_id` and `account_partial_reconcile.credit_move_id` references, verifies PostgreSQL rejects all three missing-reference conditions through the target schema, and verifies no orphan account, tax relation or reconciliation endpoint remains after rollback. It also corrupts checksum metadata on one imported accounting attachment and feeds the attachment importer source metadata pointing to a missing filestore file; the stage verifies both evidence-integrity detectors fire, rolls back and verifies clean state. The latest targeted run passed with `baseline_duplicate_groups = 0`, `injected_duplicate_groups = 1`, `final_duplicate_groups = 0`, `baseline_unbalanced_groups = 0`, `injected_unbalanced_groups = 1`, `final_unbalanced_groups = 0`, `baseline_missing_account_lines = 0`, `final_missing_account_lines = 0`, `baseline_missing_tax_relations = 0`, `final_missing_tax_relations = 0`, `baseline_incomplete_reconciliations = 0`, `final_incomplete_reconciliations = 0`, `baseline_attachment_metadata_mismatches = 0`, `injected_attachment_metadata_mismatches = 1`, `final_attachment_metadata_mismatches = 0`, `baseline_missing_file_discrepancies = 0`, `injected_missing_file_discrepancies = 1`, `final_missing_file_discrepancies = 0` and `signature_matches_after_rollback = true`.
- The harness now includes a non-destructive source-package failure-test stage. `make accounting-failure-tests` writes `artifacts/accounting-compat/private/failure-tests-status.json` and validates `6` source-package guardrails: missing source directory, missing `dump.sql`, missing filestore directory, filestore path that is not a directory, unsupported dump format and a minimal valid plain-SQL package. The stage is included in `make accounting-compat` and does not touch the real source dump or target database.
- `rebuild_account_migration.group_rebuild_accountant_reviewer` now provides a USL-specific accountant review role over Odoo's accounting read-only group. Harness checks prove read access to imported reports/discrepancies/source move, document-regeneration case, source move-line, payment, reconciliation-boundary workflow review records, source report catalogue records, external report values, imported accounting attachment metadata and one sampled binary evidence file; they also prove Trial Balance XLSX export, USL/USL Media company isolation, company-scoped document-case and external-value access, blocked writes to imported posted moves and `AccessError` on a rollback-only private technical attachment linked outside accounting.
- `custom-addons/rebuild_account_migration/tests/` now provides permanent Odoo transaction tests for accountant read-only ACL behavior, external report-value review/provenance handling, imported accounting attachment readability, private technical attachment blocking, report export metadata, report preview metadata, report-launcher action contexts, FEC/statutory filter guardrails, FEC preview guardrails, reconciliation review drill-down domains, non-posted source move-line review preservation, document-regeneration case classification/access, ECB parsing/upsert idempotence, source-rate preservation and provider-menu permissions. A scoped disposable-database run with `--test-tags=rebuild_account_migration_unit` exited with status `0`.

## Missing work

- The exact-target importer covers posted-ledger replay, review-only representation of non-posted source moves and source lines, an audited document-regeneration case workbench, and draft generation for all `189` candidate-ready non-posted cases. That exact-target draft mode still reconstructs preserved accounting lines and is not presented as tax-engine proof. Track B proves native current-period expenses and all `284` source invoices, bills, supplier refunds and purchase receipts from `2025-10-01` through `2026-06-30`, while the disposable hybrid candidate now integrates those records with exact benchmark history and has passed manager/reviewer report, bill and expense journeys. Deliberate draft/post-cutoff boundary acceptance, professional acceptance of the EUR `2.64` native exchange profit-and-loss difference and explicit candidate promotion remain separate work.
- The current report views, launcher actions and preview/export wizard are not full Enterprise `account.report` parity and do not yet satisfy all statutory/French report semantics, statutory PDF layouts or form-specific export requirements. The source report catalogue now has technical Level 4 evidence-partial packages for all `38` active source reports, but no source report has final accountant acceptance. The three PCG 2024 French variants are mapped to explicit target actions with `pcg_2024_pre_2025_opening_year` metadata, while the two association variants are catalogued as removed from the USL SASU target scope pending stakeholder/accountant acceptance.
- The local Community boundary is confirmed in code: `addons/account/models/account_report.py` contains the shared `account.report` data model and expression machinery, while `addons/account/views/res_config_settings_views.xml` exposes `module_account_reports` as an `upgrade_boolean`; no `account_reports` addon exists in this fork. Full report parity needs a lawful original USL reporting implementation or a maintained compatible dependency, not copied Enterprise code.
- French annual statements currently use USL-specific account-code mappings over imported balances; they remain technical reconstruction evidence until accountant-reviewed and expanded into the final accepted statutory package.
- The accountant-review workflow now has durable review-decision records, a sampled attachment privacy test and rollback-only native reconciliation probe evidence, but no complete external accountant onboarding package exists yet and no professional acceptance has been recorded.
- The source ledger's benchmark profile (`2` historical sequence gaps and `3` sequence-ordered date decreases) and full-snapshot profile (`16` gaps and `104` decreases) are preserved exactly and technically validated, but their business cause and acceptability still require accountant review.
- The local FEC structural preflight and DGFiP source-validation route now pass, but FEC accountant review remains open. The checked DGFiP release is `1.00.10b` from [DGFiP's official FEC test-tool page](https://www.economie.gouv.fr/dgfip/outil-de-test-des-fichiers-des-ecritures-comptables-fec). Because the packaged validators are Windows executables and the current host is `Darwin arm64` with no `wine` or `wine64`, the harness runs the CeCILL-licensed DGFiP source in an isolated Debian Bookworm container. The source tree is mounted read-only, `trt_txt.pl` exits `0`, the DGFiP log contains only `24` informational rows and `0` blocking rows, and the generated validator PDF/log bundle is archived under `artifacts/accounting-compat/private/fec-dgfip-source-validation/`.
- The CERFA/CA12/corporate-income-tax mapping is only a first ledger-derived review implementation. External declaration values can now be represented as Odoo records, but exact official box mapping, reduced-rate IS eligibility and ceiling, reintegrations, deductions, deficits and accountant-accepted declaration values remain open.
- Permanent regression coverage has started, including addon transaction tests, source-package failure guardrails, repeated target-import idempotence checks, rollback-only duplicate source-trace conflict injection, rollback-only unbalanced posted-move injection, missing-account FK rejection, missing-tax FK rejection, incomplete-reconciliation FK rejection, imported attachment checksum-metadata corruption detection and source-metadata-driven missing-file discrepancy creation, but still needs to expand into committed production-derived reconstruction fixtures, report comparison and FEC coverage.

## Uncertain work

- Source SaaS accounting/report models include Enterprise and SaaS-only tables that are not available in this Community fork.
- Active report usage requires accountant or operator confirmation after source report inventory; the current catalogue is complete as source metadata but not yet approved as final scope.
- Loan and DES parity still need scoping from actual source usage. Deferred schedules, fixed-asset schedules and EC/OSS report availability are now imported/represented, but remain accountant-review evidence until statutory and filing semantics are accepted.
- French PCG statement-version handling must remain a P0 risk until accountant-reviewed against the applicable ANC versions.
- The benchmark source has VAT account balances in the locked year, but posted tax-grid tags are primarily in the current period; final VAT/CA12 parity still needs accountant review. The deductible VAT goods/services declaration amount `1,960.00` is now traced to the posted source CA12 clearing entry, while gross account `445660` turnover `3,014.09` remains visible as ledger evidence.

## Conflicting work

- The current local bootstrap is intentionally synthetic and single-company; it cannot be used as production-derived accounting evidence.
- The Odoo Online dump requires PostgreSQL extensions and index semantics not provided by the default `postgres:16-bookworm` image.
- A clean Community target initializes generic chart-template accounts; exact replay quarantines colliding untraced bootstrap account codes so source account identity owns the chart namespace.

## Assumptions requiring revalidation

- The source backup is Odoo Online Enterprise `saas~19.2`.
- USL benchmark period `2024-01-10` through `2025-09-30` is the immutable first benchmark for reconstruction and report parity.
- Production lock dates are represented in source company fields and must be confirmed by SQL inventory.
- No production migration is authorized by this harness alone.
- The current benchmark FEC is generated in Odoo test mode, reconciles to the imported ledger, passes the local structural preflight and passes the DGFiP Test Compta Demat source-validation route. Accountant review and final FEC acceptance remain open.
