<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_move_form_expense_batch" model="ir.ui.view">
        <field name="name">account.move.form.expense.batch</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_expense_batch"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-folder-open"
                        invisible="not expense_batch_id">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Expense Batch</span>
                    </div>
                </button>
            </xpath>
            <xpath expr="//field[@name='tax_cash_basis_origin_move_id']" position="before">
                <field name="expense_batch_id"
                       readonly="1"
                       invisible="not expense_batch_id"/>
            </xpath>
        </field>
    </record>

    <record id="view_account_move_list_expense_batch" model="ir.ui.view">
        <field name="name">account.move.list.expense.batch</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='ref']" position="after">
                <field name="expense_batch_id" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
