<odoo>
    <record id="project_task_form_planning" model="ir.ui.view">
        <field name="name">project.task.form.planning.compatibility</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <label for="date_deadline" position="before">
                <field name="planned_date_begin"/>
            </label>
            <div id="date_deadline_and_recurring_task" position="after">
                <field name="usl_dependency_date_warning"
                       class="text-warning"
                       readonly="1"
                       nolabel="1"
                       colspan="2"
                       invisible="not usl_dependency_date_warning"/>
            </div>
            <notebook position="before">
                <section class="o_usl_task_stage_duration" invisible="not usl_stage_duration_history">
                    <div class="o_usl_task_stage_duration_header">
                        <div>
                            <h3>Time spent by stage</h3>
                            <p>Preserved totals include every current and historical project column.</p>
                        </div>
                        <i class="fa fa-clock-o" title="Stage duration history" aria-hidden="true"/>
                    </div>
                    <field name="usl_stage_duration_history" widget="html" readonly="1" nolabel="1"/>
                </section>
            </notebook>
        </field>
    </record>

    <record id="project_task_list_planning" model="ir.ui.view">
        <field name="name">project.task.list.planning.compatibility</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_tree2"/>
        <field name="arch" type="xml">
            <field name="date_deadline" position="before">
                <field name="planned_date_begin" optional="hide"/>
            </field>
        </field>
    </record>
</odoo>
