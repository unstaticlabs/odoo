<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Live electronic-invoicing exchange is an explicit production action.
        Module installation, database initialization and upgrades must never
        register, fetch, send or report data.
    -->
    <function model="res.company"
        name="_rebuild_apply_default_einvoice_provider"/>

</odoo>
