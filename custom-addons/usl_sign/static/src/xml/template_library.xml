<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="usl_sign.TemplateKanban.Buttons" t-inherit="web.KanbanView.Buttons" t-inherit-mode="primary">
        <xpath expr="//button[hasclass('o-kanban-button-new')]" position="replace">
            <input type="file" multiple="true" accept="application/pdf,.pdf" t-ref="uploadInput" class="d-none" t-on-change.stop="onInputChange"/>
            <button t-if="!env.inDialog and canCreate" type="button" class="btn btn-primary" t-att-disabled="uploadState.uploading" t-on-click="createRecord">
                <i t-att-class="uploadState.uploading ? 'fa fa-circle-o-notch fa-spin me-1' : 'fa fa-upload me-1'"/>
                <t t-if="uploadState.uploading">Preparing…</t><t t-else="">Upload PDF</t>
            </button>
        </xpath>
        <xpath expr="." position="inside">
            <button type="button" class="btn btn-secondary" t-on-click="openCompletedDocuments">Completed documents</button>
            <button type="button" class="btn btn-secondary" t-on-click="openDecisions">Decisions</button>
        </xpath>
    </t>
</templates>
